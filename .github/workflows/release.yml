#The name of the release
name: Release v1.0.7
#Declaring the branches and tags
on:
  push:
    branches:
      - "main"
#Declaring all jobs and flows
jobs:
  release:
    name: Release v1.0.7
    if: "github.actor != 'github-actions[bot]' && !contains(github.event.head_commit.message, '[sonar]') && !contains(github.event.head_commit.message, '[skip ci]')"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      #Checkout Code
      - name: Checkout code
        uses: actions/checkout@v3

      #Create New Release
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        with:
          tag_name: v1.0.7
          release_name: Iqraa v1.0.7
          body: |
            Changes in this Release:
              - [New] Release new version v1.0.7
              - [New] Added SonarQube
          draft: false
          prerelease: false